import numpy as np
import matplotlib.pyplot as plt 

#needed to do pip install ___ and select python interpreter based on where installed (I made venv)

# to integrate into website, use Streamlit. use user input to generate graphs/visualizations. 
